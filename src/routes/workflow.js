const express = require('express');
const router = express.Router();

// Placeholder for workflow routes
router.get('/', (req, res) => {
  res.json({ message: 'Workflow endpoints - Coming soon' });
});

module.exports = router;